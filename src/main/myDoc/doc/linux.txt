删除文件       		   rm -f(不提示用户)
删除非空目录 		   rm -rf cmake-2.8.4
查看mysql版本              mysql --version
yum安装的mysql卸载         yum remove mysql
强制删除非空目录           rm -rf ./mysql
securtCRT中上传和下载      rz  sz
yum安装                    yum install
解压tar.gz到指定位置       tar -zxvf abc.tar.gz -C /home/test/

gcc g++
kernel-devlel  gcc-devel
make -j{x}  使用多少条线程进行编译

在bin目录 ./httpd.sh stop start






1:安装j2ee容器&容器的配置、优化、安全
2:上传编译好的站点
3:将站点放入容器
4:启动容器


1.Linux基础
a.文档管理（目录结构，查看，新建，修改，删除，复制，移动）
b.文档的属性（777，权限）
c.系统的配置（计算机名，网络，防火墙，系统维护）
d.软件的安装、卸载（通过网络安装 centos:yum, ubuntu:apt-get, 通过rpm安装,rpm -ivh xx.rpm,  tar.gz(源码)）


下载源码包
解压
配置--解决依赖
编译--解决依赖
安装


php -->>  libpng

shell{}